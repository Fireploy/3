<nav class="navbar navbar-expand-lg fixed-top" style="background-color: #c82333;" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/dashboard">
            <img src="/images/Logo-nuevo-horizontal.png" alt="Logo UFPS" height="30" class="d-inline-block align-text-top me-2">
            ACTISIS
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link <%= (typeof page !== 'undefined' && page === 'dashboard') ? 'active' : '' %>" href="/dashboard">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (typeof page !== 'undefined' && page === 'registro_actividad') ? 'active' : '' %>" href="/registro_actividad">Registrar Actividad</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (typeof page !== 'undefined' && page === 'consultar_actividades') ? 'active' : '' %>" href="/consultar_actividades">Consultar Actividades</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (typeof page !== 'undefined' && page === 'generar_reporte') ? 'active' : '' %>" href="/generar_reporte">Generar Reporte</a>
                </li>
                <% if (typeof user !== 'undefined' && user && user.role === 'admin') { %>
                    <li class="nav-item">
                        <a class="nav-link <%= (typeof page !== 'undefined' && page === 'gestionar_usuarios') ? 'active' : '' %>" href="/gestionar_usuarios">Gestionar Usuarios</a>
                    </li>
                <% } %>
            </ul>
            <ul class="navbar-nav ms-auto">
                 <% if (typeof user !== 'undefined' && user) { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-person-circle me-1" viewBox="0 0 16 16">
                                <path d="M11 6a3 3 0 1 1-6 0 3 3 0 0 1 6 0"/>
                                <path fill-rule="evenodd" d="M0 8a8 8 0 1 1 16 0A8 8 0 0 1 0 8m8-7a7 7 0 0 0-5.468 11.37C3.242 11.226 4.805 10 8 10s4.757 1.225 5.468 2.37A7 7 0 0 0 8 1"/>
                            </svg>
                            <%= user.email %>
                            <% if (user.role === 'admin') { %>
                                <span class="badge bg-light text-danger">Admin</span>
                            <% } else if (user.role === 'docente') { %>
                                <span class="badge bg-light text-primary">Docente</span>
                            <% } else { %>
                                <span class="badge bg-light text-success">Usuario</span>
                            <% } %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/mi_perfil">Mi Perfil</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <button id="logout-button" class="dropdown-item">Cerrar Sesión</button>
                            </li>
                        </ul>
                    </li>
                 <% } else { %>
                     <li class="nav-item">
                        <a class="nav-link" href="/login">Iniciar Sesión</a>
                    </li>
                 <% } %>
            </ul>
        </div>
    </div>
</nav>